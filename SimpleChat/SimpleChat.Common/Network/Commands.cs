namespace SimpleChat.Common.Network
{
    public enum Commands
    {
        SET_USER = 1,
        SET_USER_OK,
        SET_USER_FAIL,
        MESSAGE,
        DISCONNECT,
        SERVER_STOPPED,
        ERROR
    }
}
